# o3tv-feed

Generated data for O3TV — no source code here, regenerated daily by
`oumarbarry/o3tv-data`'s CI. Served via GitHub Pages:

- `channels.json` — scored channel list (contract: `{version, generatedAt, channels[]}`)
- `epg/<channelId>.json` — per-channel EPG programs where available
